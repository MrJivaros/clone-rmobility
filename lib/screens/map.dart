import 'package:flutter/material.dart';

class MapEndStatisticScreen extends StatelessWidget {
  static const routeName = '/mapEndStatistic';
  const MapEndStatisticScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
